{
  "name": "backend",
  "version": "1.0.0",
  "description": "The server (mostly rest) for verifying identity",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sqlite3": "^4.1.1"
  }
}
